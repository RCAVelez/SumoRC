from setuptools import setup

setup(
    name="simulation",
    version='0.0.1',
    install_requires=['gym',
                      'pybullet',
                      'numpy',
                     ]
)
